<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yihukurama.cartoolsc">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".CarToolsC"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.BaseActivity" />
        <activity android:name=".view.activity.secactivity.DaohanActivity" />
        <activity android:name=".view.activity.secactivity.CheliangfuwuActivity" />
        <activity android:name=".view.activity.secactivity.YaokongActivity" />
        <activity android:name=".view.activity.secactivity.GestureActivity" />
        <activity android:name=".view.activity.secactivity.ShezhiActivity"></activity>
    </application>

</manifest>
